<div class="login-container">
  <h2>ログイン</h2>

  <%= form_with(scope: :session, url: login_path, local: true, class: "login-form") do |f| %>
    <div class="form-group">
      <%= f.email_field :email, class: "form-control js-input", placeholder: " " %>
      <%= f.label :email, "email" %>
    </div>

    <div class="form-group">
      <%= f.password_field :password, class: "form-control js-input", placeholder: " " %>
      <%= f.label :password, "password" %>
    </div>

    <div class="form-actions">
      <a href="#" class="btn btn-svg" onclick="document.querySelector('.login-form').submit(); return false;" aria-label="ログインフォームを送信">
        <svg>
          <rect x="2" y="2" rx="0" fill="none" width="200" height="50"></rect>
        </svg>
        <span>ログイン</span>
      </a>
    </div>
  <% end %>
</div>

